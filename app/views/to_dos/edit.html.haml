%link{ :href => "//netdna.bootstrapcdn.com/bootstrap/3.1.1/css/bootstrap.min.css", :type => "text/css", :rel => "stylesheet" }

%h1 Edit Task

= form_for @to_do, url: "/to_dos/#{@to_do.id}" do |f|
	= render 'form', f: f

%br
= link_to 'Show', to_do_path
\|
= link_to 'Back', to_dos_path
